# sanctum-docs uses INTENTIONAL documentation placeholders (per CONTRIBUTING.md:
# bridge 10.0.0.x, LAN 192.0.2.x, tailnet 100.0.0.x, RFC-5737 example ranges) and
# is leak-gated by scripts/contrib-check.py — NOT the code-repo no-hardcoded-ip
# resolver rule (prose can't `source endpoints.env`). Exempt the content + data.
src/content/docs/*
src/data/*
# CONTRIBUTING.md is the file that DOCUMENTS the IP-placeholder policy above —
# it necessarily contains the example ranges it describes. Exempt it too.
CONTRIBUTING.md
