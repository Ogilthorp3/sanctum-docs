---
title: The Smart Router
description: How sanctum-server learned to stop worrying and route to the right brain.
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

![The Smart Router — three neural pathways branching from a central node, each leading to a different brain.](./images/hero-smart-router.jpg)

# The Smart Router

**Date:** 2026-04-07
**Status:** Production (sanctum-server v0.2.0)

It turns out that sending a coding question to a model trained exclusively on Jedi personas produces... unhelpful results. Who knew.

The Smart Router extends `sanctum-server` (Rust/Axum) with multi-backend dispatch. One port, multiple brains. Requests are routed based on the `model` field, glob patterns, or intent classification from the prompt content.

## Architecture

```
Client Request (:1337)
    │
    ▼
┌─────────────────────────┐
│   sanctum-server         │
│   ┌───────────────────┐ │
│   │   Smart Router     │ │
│   │                     │ │
│   │  1. Exact match     │ │
│   │  2. Glob pattern    │ │
│   │  3. Intent keywords │ │
│   │  4. Default          │ │
│   └───────┬─────────────┘ │
└───────────┼───────────────┘
            │
    ┌───────┼───────┐
    ▼       ▼       ▼
 Council   Coder   Claude
 (1337)    (1234)  (2001)
```

## Routing Tiers

| Tier | Mechanism | Example | Latency |
|------|-----------|---------|---------|
| 1 | Exact model name | `"model": "coder"` → Coder backend | 0ms |
| 2 | Glob pattern | `"model": "council-heartbeat"` matches `council-*` | 0ms |
| 3 | Intent keywords | Prompt contains "debug" + "python" → Coder | ~1ms |
| 4 | Default | No match → Council (configurable) | 0ms |

## Configuration

Router config lives in `~/.sanctum/instance.yaml` under the `router:` key:

```yaml
router:
  backends:
    council:
      url: http://127.0.0.1:1337/v1
      models: ["council-*", "qwopus", "heartbeat"]
    coder:
      url: http://127.0.0.1:1234/v1
      models: ["coder", "code-*", "*coder*"]
    claude:
      url: http://127.0.0.1:2001/v1
      models: ["claude-*", "opus"]
  default: council
  intent_keywords:
    coder: ["function", "debug", "refactor", "rust", "python"]
    council: ["yoda", "windu", "lights", "alarm"]
    claude: ["explain why", "analyze", "summarize"]
```

## Endpoints

| Endpoint | Description |
|----------|-------------|
| `POST /v1/chat/completions` | Routed chat — selects backend automatically |
| `GET /v1/models` | Lists all registered backends |
| `GET /v1/backends` | Backend names + patterns |
| `GET /health` | Reports `"mode": "routed"` or `"single"` |

## Implementation

The router is a new Rust module in `sanctum-server/src/router.rs`. The `HttpProxyBackend` in `backend.rs` handles the actual proxying to any OpenAI-compatible endpoint, with automatic detection of streaming (SSE) vs non-streaming (JSON) responses.

Non-quantized and quantized backends both work. The proxy handles think-tag stripping (`strip_thinking` flag) regardless of which backend processes the request.

<Aside type="tip">
The `/v1/backends` endpoint is useful for debugging. Hit it when you're not sure which brain is answering.
</Aside>

## Tests

23 tests total: 18 unit (pattern matching, think-tag stripping) + 5 e2e (mock backends, full binary, pattern routing, intent classification, default fallback).

```bash
cd sanctum-server && cargo test
```

The e2e tests spin up 3 mock Axum servers, launch the actual `sanctum-server` binary with a temp config, and verify requests land on the correct backend. Because trust, but verify.
