---
title: Jocasta MCP (The Archivist)
description: The master Model Context Protocol server exposing the entirety of the user's personal communications, calendar, unstructured thoughts, and iCloud documents purely via offline macOS databases.
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

![Jocasta MCP — a vast library of floating data crystals and holographic scrolls weaving a unified data stream.](./images/hero-jocasta-mcp.png)

Jocasta is the ultimate local-first AI data gateway. Rather than relying on cloud API scraping or headless browser hacks (which are notoriously brittle), `sanctum-jocasta-mcp` connects directly to the underlying `SQLite` databases running silently on the Mac Mini host. It exposes 16 discrete tool sets that are shared centrally among the Jedi Council.

Because all the databases queried (Messages, iOS Notes, Health) live entirely offline under `~/Library`, data egress costs and latency are basically zero. 

## The Architectural Design
The MCP operates through two unified streams:

1. **The MCP Server (`index.ts`)**: For autonomous LLM agents speaking standard `.stdio` RPC.
2. **The Terminal CLI (`cli.ts`)**: For SSH automation and bare Bash scripting (`npx sanctum-jocasta <command>`).

## Complete Unified Capabilities (The 16 Connectors)

<CardGrid>
  <Card title="Apple Mail" icon="email">
    Reads `~/Library/Mail/V*` and queries the primary `Envelope Index`. It maps sender aliases and email subjects instantaneously.
  </Card>
  <Card title="Messages & Protocols" icon="comment">
    Seamlessly parses `chat.db` for natively synced iMessage and SMS. Also hooks straight into Catalyst bundles for **WhatsApp** and **Telegram**, and decodes the `config.json` master key to decrypt **Signal's** local SQLCipher `db.sqlite`.
  </Card>
  <Card title="Electron Bundles" icon="warning">
    **Slack** and **Discord** data is heavily blocked due to Electron's use of LevelDB packet chunks (`Local Storage/leveldb`). Those tools respond with graceful failures for now.
  </Card>
  <Card title="Apple Core Data" icon="apple">
    Jocasta queries `NoteStore.sqlite` for uncompressed **Apple Notes** brainstorming, extracts iOS/Mac **Calendar & Reminders* schedules out of `Calendar.sqlitedb`, and natively parses `AddressBook.abcddb` to perform **CRM correlation** of cell numbers to actual names effortlessly.
  </Card>
  <Card title="Offline Knowledge" icon="open-book">
    Provides access to Wikipedia and encyclopedias by proxying requests securely to a local `.zim` unzipper (`kiwix-serve`).
  </Card>
  <Card title="GPS & Media" icon="magnifier">
    Queries the master `Photos.sqlite` for exact timestamp and location metadata of media. Integrates with OpenStreetMap to reverse-geocode those latitudes/longitudes seamlessly.
  </Card>
</CardGrid>

## Active Operations (The Native executioner's blade)
Because Jocasta operates via secure `sqlite3.OPEN_READONLY` connections for speed, she utilizes a separate isolated AppleScript pipeline (`osascript`) to enact destructive actions securely. This avoids Core Data corruption natively:
- **`jocasta_write_note`**: Scaffolds an organized document in your default iCloud Apple Notes directory natively.
- **`jocasta_write_calendar`**: Ingests ISO datetimes to programmatically schedule meetings and events directly in the Calendar app without manual scraping.
- **`jocasta_send_imessage`**: Invokes the Mac's background Messages daemon to dispatch iMessages/SMS seamlessly.
  - *Primary Use Case*: Mon Mothma utilizes this tool as a PagerDuty pipeline. If a service drops, she can dispatch a text message *directly to your own phone number*, guaranteeing the alert hits your lock screen.

## Web Browsing (Agent-Browser)

Rather than embedding a bloated Playwright container inside the Node runtime, Jocasta natively proxies automation requests down to the user's global `agent-browser` (Chrome via CDP).
- **Tool**: `jocasta_agent_browser`
- By passing strings directly (like `open https://github.com && snapshot -i`), Jocasta handles DOM inspection and downloading files via refs magically.
- **Security Check**: This process explicitly launches heavily bounded via the `AGENT_BROWSER_ALLOWED_DOMAINS` env var. She cannot explore arbitrary websites, thereby locking out heavy prompt-injection risks!

<Aside type="caution">
  **The Keychain Risk**: Jocasta can execute `security find-internet-password`. This throws a native Touch ID/macOS access pop-up the first try. It is inherently dangerous to give an AI plaintext passwords! Where possible, users should utilize the `agent-browser auth login` vault pattern instead to abstract secrets away from the LLM context.
</Aside>

## Document Parsing and Telemetry (Anti-Sith Army)

For systems failure tracking and massive unstructured document ingest:

1. **Jocasta Sysmon** (`jocasta_read_syslog` & `jocasta_read_process_list`): Exposes raw memory profiling (unix `ps`) and macOS Unified Logs (`log show --predicate`). To protect the LLM context window, these outputs are hard-truncated precisely to 500 records.
2. **Universal Files** (`jocasta_read_file`): Equipped with `pdf-parse` and `mammoth`, Jocasta can slice through generic `.docx`, `.pdf`, `.md`, and `.csv` items downloaded into iCloud.

## Testing and E2E

Like `.mlx` components, Jocasta asserts viability with an explicitly built `test_e2e_jocasta.sh`. The test builds the TypeScript outputs cleanly, calls all Core CLI functions checking for intact `stdout`/`stderr` structures, tests telemetry, and gracefully evaluates unknown paths for exception management.
