---
title: Architecture Overview
description: How Sanctum's Mac Mini host and Ubuntu VM work together to power a haus intelligence platform.
---

import { Card, CardGrid, Aside, Tabs, TabItem } from '@astrojs/starlight/components';
import ZoomImage from '../../../components/ZoomImage.astro';
import networkTopology from './images/sanctum-network-topology.svg';

Sanctum runs on a two-layer architecture: a **Mac Mini host** that manages hardware, networking, and Apple-native services, and an **Ubuntu VM** that runs the AI agent cluster. The two communicate over a private Host-Only network and are managed through a single configuration file.

![Mac Mini cutaway — two worlds in one aluminum rectangle](./images/mac-mini-cutaway.png)

If you're wondering why anyone would build a distributed intelligence platform on consumer hardware tucked behind a residential ISP -- congratulations, you've found the right documentation. Keep reading. The answers won't comfort you.

## System Diagram

<ZoomImage src={networkTopology.src} alt="Sanctum network topology — Mac Mini host and Ubuntu VM connected via bridge100" />

Stare at that diagram long enough and two things become clear: first, this is a real system that actually runs a household. Second, no one should have let it get this far. And yet here we are, port 1984 deep, with no intention of stopping.

The platform uses a **3-tier model routing** architecture: Cloud (Opus 4.6), Local Ops (Coder-14B), and Local Secure (Gemma4+LoRA). The Smart Router dispatches each agent to the right tier based on Carmack Olympics benchmark results. A **Model Tournament** system automates evaluation and deployment of new models -- 178 tests across 11 components keep the whole thing honest.

## Mac Mini Host

The Mac Mini is the always-on hub. It runs macOS with over 20 LaunchAgents that start on boot, a Docker instance for Home Assistant, and several AI inference servers that take advantage of Apple Silicon. It is also, let's be honest, a $1,600 space heater that happens to control the other space heaters.

<CardGrid>
  <Card title="DenchClaw Gateway" icon="rocket">
    The Mac-side agent gateway. Runs Jocasta, the Archivist — keeper of CRM data and communications. Exposes port 1977 for local clients and the Holocron chat interface on port 1977.
  </Card>
  <Card title="Home Assistant" icon="approve-check-circle">
    Docker container with bridge networking. Controls lights and sensors. Sonos speakers are managed by a native bridge (port 18421). Accessible at port 8123 locally and via Cloudflare tunnel externally.
  </Card>
  <Card title="AI Inference (3-Tier)" icon="star">
    Three tiers, ranked by Carmack Olympics benchmarks. Cloud: Opus 4.6 via proxy on :4040 with daily cost caps. Local Ops: Coder-14B on LM Studio (:1234) for infrastructure and coding. Local Secure: Gemma4+LoRA on sanctum-mlx (:1337) for security and domain tasks. XTTS (:8008) handles text-to-speech on the MPS GPU. The Model Tournament automates eval and deploy of new models across all tiers.
  </Card>
  <Card title="Triage Daemon (Rust)" icon="random">
    Native memory sentinel that monitors system pressure. Automatically kills Apple bloatware and unloads large models when RAM drops below 20%.
  </Card>
</CardGrid>



### Boot Sequence

All LaunchAgents use `RunAtLoad: true` and start in this order. Every boot is a small miracle. Twenty-some services waking up in sequence, each one assuming the last one did its job. It works like a row of dominoes -- except some of the dominoes are SSH tunnels and one of them controls your front door.

| Order | Service | What It Does |
|-------|---------|-------------|
| 1 | VM Autostart (QEMU headless) | Launches QEMU headless, starts the VM, sets bridge100 IP to 10.10.10.1 |
| 2 | DenchClaw Gateway | Mac agent gateway on port 1977 |
| 3 | Firewalla Bridge | Router API proxy on port 1984 |
| 4 | XTTS TTS Server | Voice synthesis on port 8008 (MPS GPU) |
| 5 | Voice Agent | Yoda voice interface on port 1138 |
| 6 | HA SSH Tunnel | Forwards port 4007 to VM for HA integrations |
| 7 | Council-27B MLX | Local LLM with LoRA adapters on port 1337 |
| 7a | Dynamic Wrapper | Dynamic paths via [Dynamic Model Routing](/architecture/dynamic-model-routing/) |
| 8 | Cloudflare Tunnel | Exposes services via `example.net` subdomains |
| 9 | Triage Daemon | Native Rust memory sentinel (Qui-Gon immune response) |
| 10 | iCloud Filer | Auto-files documents from iCloud Drive |
| 11 | Sanctum Proxy | AI routing proxy on port 4040 |
| 12 | Orbi Bridge | Socat bridge to Orbi router |
| 13 | Command Center | Dashboard on port 1111 |
| 14 | Health Center | Health monitoring dashboard |
| 15 | LM Studio | LLM inference server on port 1234 |
| 16 | iCloud Backup | Periodic backup agent |
| 17 | Watchdog | Health checks every 600 seconds, auto-heals via service-doctor |

<Aside type="tip">
  The full LaunchAgent inventory is in the [LaunchAgents reference](/reference/launchagents/). The watchdog monitors all services and can restart most of them automatically -- which it will need to do, because entropy is undefeated.
</Aside>

## Ubuntu VM

The VM is a QEMU guest running Ubuntu 24.04 with Apple Hypervisor acceleration. It has no direct internet access -- all external communication routes through the Mac host at 10.10.10.1.

This is by design. An air-gapped VM running six AI agents that can only see the outside world through a single bridge interface is either excellent security architecture or the plot of a containment thriller. Possibly both.

### VM Specifications

| Parameter | Value |
|-----------|-------|
| Hypervisor | QEMU with Apple Hypervisor |
| OS | Ubuntu 24.04 LTS |
| CPU | 4 cores |
| Memory | 8 GB |
| Storage | 34 GB qcow2 |
| Network | Host-Only (vmnet), bridge100 |
| VM IP | 10.10.10.10 |
| SSH | `ssh openclaw` or `ssh ubuntu@10.10.10.10` |

### Agent Cluster

The VM runs OpenClaw with six specialized agents, each with a distinct role. Yes, someone named their haus automation agents after Star Wars characters. No, there was never a serious alternative under consideration.

| Agent | Role | Description |
|-------|------|-------------|
| **Yoda** | Grand Master | Senior lead of the Sanctum Council. High-level orchestration, council routing, and final synthesis of complex decisions. |
| **Mon Mothma** | Operations | Orchestrator of Force Flow and Living Force. Manages incident correlation, boot sequences, and cross-domain signal alignment. |
| **Qui-Gon** | Infrastructure | Guardian of the machine. System health, Docker stability, and automated recovery. |
| **Windu** | Security | Master of defense. Security audits, Firewalla and PF rules, perimeter monitoring. |
| **Cilghal** | Health | Biological bridge. Wellness monitoring via Apple Health, cognitive profile analysis, and productivity scaffolding. |
| **Ki-Adi-Mundi** | Finance | Treasurer. Triptyq Capital deal flow and personal fiscal health. |

The gateway runs as a systemd user service (`openclaw-gateway.service`) with a 1.5 GB heap limit. Secrets are SOPS+age encrypted and decrypted at startup via a wrapper script.

## Host-Only Network

The Mac and VM communicate over a private bridge100 interface on the 10.10.10.0/24 subnet. This design keeps the VM completely air-gapped from the internet while allowing full-speed access to Mac services.

```
Mac Mini (10.10.10.1)  <----  bridge100  ---->  VM (10.10.10.10)
```

- **Mac to VM**: Direct SSH, direct access to all VM ports
- **VM to Mac**: Direct access to all Mac ports on 10.10.10.1
- **VM to Internet**: Not possible. The VM relies on the Mac for any external data (synced via rsync, git pull, or API proxies).

<Aside type="caution">
  The bridge100 IP (10.10.10.1) is set by the VM autostart script using `sudo ifconfig`. A sudoers entry at `/etc/sudoers.d/vmnet-bridge` allows this without a password prompt. If that sudoers file ever disappears, the VM wakes up into a world with no bridge to anywhere. It will handle this about as well as you'd expect.
</Aside>

## External Access

Two mechanisms provide access from outside the local network:

### Cloudflare Tunnel

The `sanctum-hub` tunnel exposes select services under the `example.net` domain with Cloudflare Zero Trust:

| Subdomain | Target |
|-----------|--------|
| `health.example.net` | `localhost:10101` |
| `ha.example.net` | `localhost:8123` |

### Tailscale

All Sanctum nodes join a shared Tailscale tailnet. This provides encrypted mesh networking for SSH, API access, and inter-node communication without opening any ports on the home router. It's the one piece of infrastructure here that someone else maintains, and that fact brings genuine peace.

## Skills and Plugins

Agents extend their capabilities through skills and plugins:

- **Skills** are executable scripts organized by domain (e.g., `firewalla-toolkit`, `apple-toolkit`, `house-pulse`). They live in a shared Git repository and sync from the Mac to the VM every 30 minutes via cron.
- **Plugins** provide persistent integrations. The current set includes sanctum-memory on :42069 with Jina v2 reranking on :42070. All data stays on-device — no cloud memory services.

<Aside>
  For the full service catalog with ports and LaunchAgent names, see [Services](/architecture/services/). For configuration details, see [Config System](/architecture/config-system/).
</Aside>
