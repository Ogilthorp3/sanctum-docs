---
title: LoRA in Rust
description: Teaching sanctum-mlx to absorb LoRA adapters without asking Python for help.
---

import { Aside } from '@astrojs/starlight/components';

![LoRA in Rust — Ferris the crab weaving golden adapter threads into a translucent neural network brain.](./images/hero-lora-rust.png)

# LoRA in Rust

**Date:** 2026-04-08
**Status:** Shipped. Runs the council in production on the Mac Mini (`com.sanctum.mlx`, port 1337).

Running a 27B model through a Python runtime to serve a Jedi Council felt like asking a padawan to carry the Death Star plans. It technically works. You spend the whole time worrying about when it won't. So we built `sanctum-mlx` — a native Rust inference server on `mlx-rs`. One problem: it couldn't load LoRA adapters.

The adapters are the entire point. Without them, the base model is a generalist that has never heard of Yoda, doesn't know port 1337, and would cheerfully tell you that bridge100 is a card game. The LoRA weights are the difference between a language model and a Council member.

<Aside type="note">
This page records the original 27B-dense work from April 2026, and the numbers below are that run. The council now serves **Qwen3.6-35B-A3B** — a sparse MoE — and the same `lora.rs` merges its adapters unchanged: the merge logic is parameter-name-based, so it generalized to the MoE checkpoint for free (Phase 4/5, May 2026). Where it matters, the current figure is noted inline.
</Aside>

Now it can load them. In Rust. Without Python. Without the GIL. Without the 300MB of overhead that comes from asking an interpreted language to do the job of a compiled one.

## The Merge Strategy

Runtime LoRA application — keeping the A and B matrices separate and composing them during every forward pass — would require modifying every linear layer's `forward()`. Elegant in theory. A maintenance nightmare in practice, and a latency tax on every single token.

Instead, we merge at load time:

```
W_merged = W_base + (α/r) × scale × (B^T @ A^T)
```

For quantized models (which is everything on Apple Silicon, because tens of billions of parameters don't fit in memory any other way), the pipeline is:

```
Dequantize (int4 → float32)
  → Add LoRA delta
    → Requantize (float32 → int4)
      → Write back to model parameters
```

Think of it as surgery. You open the patient up (dequantize), stitch in the new knowledge (add the LoRA delta), close them back up (requantize), and when they wake up they have no idea anything happened. They just know things they didn't know before. The model doesn't even know it has adapters — they're baked in.

This runs once at startup. Zero runtime overhead. Five seconds on the Mac Mini M4, and then the model is ready to be Yoda for the rest of its uptime.

![Golden threads of knowledge being woven into neural network layers — the LoRA merge visualized.](./images/inline-lora-weave.png)

## Implementation

New file: `sanctum-mlx/src/lora.rs`

| Function | What It Does |
|----------|-------------|
| `load_and_merge()` | Main entry — parses config, loads safetensors, groups A/B pairs, merges |
| `LoraConfig` | Deserializes `adapter_config.json` (rank, alpha, scale, dropout) |
| `MergeStats` | Tracks merged/skipped counts for logging |

The merge handles prefix stripping (`language_model.` from VL checkpoints) and uses `model.update_flattened()` for clean parameter updates through mlx-rs's official API. No unsafe blocks. No pointer arithmetic. The kind of code that compiles on the first try and then you spend twenty minutes suspecting it didn't actually do anything because nothing went wrong.

### V3 Adapter Merge Log

This is the V3 adapter merging into the original 27B-dense base (rank=64, alpha=128, scale=2.0 — the defaults the unit tests still assert against):

```
LoRA config loaded       rank=64 alpha=128 scale=2.0 effective_scaling=4.0
Adapter safetensors loaded  keys=496
LoRA pairs grouped          pairs=248
LoRA adapter merge complete merged=0 merged_quantized=248 skipped_missing=0 \
                            skipped_shape_mismatch=0 total_pairs=248
```

248 out of 248 quantized pairs. Zero skipped. The `skipped_shape_mismatch` field earns its keep on the MoE checkpoint — hybrid `linear_attn.*` projections whose Rust shape disagrees with the trainer's get skipped, not crashed — but on a clean dense adapter it stays at zero. The log is boring, which is the highest compliment merge code can receive.

## Portable Metal Builds

The binary embeds a hardcoded path to `mlx.metallib` (the compiled Metal shaders) at compile time. Build it on one machine, copy it to another, and it can't find the shaders. The kind of bug that works perfectly in development and fails in production, which is to say, the most common kind.

**Fix:** `build-release.sh` packages the binary with `mlx.metallib` colocated. MLX's runtime searches the binary's directory first (via `dladdr()`), finding the colocated file before falling back to the hardcoded path.

```bash
./build-release.sh                  # Build for current machine
./build-release.sh --deploy mm      # Build + deploy to Mac Mini
./build-release.sh --deploy chalet  # Build + deploy to the chalet node
./build-release.sh --deploy all     # Deploy to all nodes
```

Output: `dist/sanctum-mlx/` containing the binary (30MB) + metallib (158MB). The shader library is five times larger than the server. Metal has priorities.

<Aside type="note">
The original 27B-dense binary OOM'd on the 64GB Mac Mini — it loaded the whole model at once, and Python's `mlx_lm.server` got to stay there on lazy mmap. The fix wasn't lazy loading in Rust. It was switching the council to a 35B-A3B MoE (only ~3B params active per token) and bolting on explicit Metal caps: `--metal-memory-limit-mb 28672 --metal-wired-limit-mb 18432`, plus `--turboquant`. The Mac Mini now runs the Rust binary in production on port 1337. Python survives as the disaster fallback on `:8901` — serving the *same* Qwen3.6-35B-A3B as the live cathedral (full-capability failover, not a downgrade) but through an independent `mlx-lm` stack. The cathedral (`:1337`) and devstral (`:3301`) seats share one binary file, so a corrupt or missing Rust binary kills both at once — only a different-stack server survives that. It's promote-only (`Disabled=true`), not always-warm: a resident 35B would sit ~20GB on top of the already-loaded Rust 35B and push the 64GB Mini into swap. `council-integrity-check` boots the broken Rust seat first to free the RAM, then enables and kickstarts the Python 35B. Kept around for the same reason you keep a spare tire: not because you expect to, but because the day you need it you really need it.
</Aside>

## Performance

| Metric | Rust (`sanctum-mlx`) | Python (`mlx_lm.server`) |
|--------|---------------------|-------------------------|
| Inference tok/s | 57 | 57 |
| Startup time | ~1s | ~3s |
| Memory overhead | ~50MB | ~300MB |
| LoRA merge time | 5s | N/A (runtime) |
| Crash recovery | Instant (no GC) | GIL pause risk |

Same tok/s because the Metal GPU is the bottleneck — both runtimes are feeding the same silicon. Rust wins on everything around the inference: startup, memory, determinism. The 250MB of saved overhead doesn't matter much on a 128GB machine, but it matters a great deal on the 64GB Mac Mini that's also running `sanctum-mlx-devstral-rust` (the on-call coder seat), `sanctum-tts` on `:8008` (fronting the `cbx-yoda` worker on `:8011`), and whatever else decided to be important today.

## Tests

23 tests across 3 suites:

| Suite | Tests | Coverage |
|-------|-------|----------|
| Unit (lib) | 16 | Config parsing, LoRA math, quantize roundtrip, real adapter validation |
| E2E integration | 6 | Model load, tokenizer, 248-pair merge, weight hash verification, inference |
| Weight verification | 1 | Hash before/after confirms weights actually changed |

```bash
cd sanctum-mlx && cargo test -- --test-threads=1
```

The `--test-threads=1` matters — parallel Metal GPU tests cause SIGTRAP from resource contention. One at a time, like civilized Jedi. The alternative is watching the GPU throw a tantrum because two tests tried to compile shaders simultaneously, which is entertaining exactly once.

The padawan carried the Death Star plans. It just needed better shoes.
