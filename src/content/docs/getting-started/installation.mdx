---
title: Installation
description: The detailed reference for installing Sanctum on your Mac. If you just want the fastest path, see Quick Start. This page is the longer walkthrough with full troubleshooting.
---

import { Steps, Aside, Tabs, TabItem, Code } from '@astrojs/starlight/components';

![Tommy supervises the Mac Mini setup — quality assurance by feline](./images/tommy-mac-mini.png)

Tommy has sat on every Mac Mini that ever ran Sanctum and rejected exactly none of them. Low bar, but he's never been wrong.

This is the detailed install reference. Never set up Sanctum and want the friendly 4-step version? Start with [Quick Start](./quick-start/) — same canonical flow, just terser. This page goes deeper: troubleshooting, a post-install verification protocol, and an operator-grade appendix for running from source.

## The canonical install path

Sanctum's public install path is **the Homebrew tap** at `Ogilthorp3/homebrew-sanctum`. The tap installs the `sanctum-cli` Python application, which orchestrates everything else — cloud backup setup, first backup, restore canary.

<Steps>

1. **Install Homebrew** if you don't already have it.

   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```

   Homebrew installs into `/opt/homebrew/` and asks for your administrator password once. That prompt is from Apple's installer, not Homebrew — it needs to create directories under `/opt/`. When it finishes, `brew --version` should print something like `Homebrew 5.x.x`.

   <Aside type="tip">
   On a brand-new Mac, the first `brew install` may also prompt you to install Apple's Command Line Tools. Let it — that's a one-time macOS thing, routed through System Settings → Software Update if your Mac doesn't have them yet.
   </Aside>

2. **Install `sanctum-cli` via the Homebrew tap.**

   ```bash
   brew install ogilthorp3/sanctum/sanctum-cli
   ```

   This installs Python 3.12 and `restic` (if you don't already have them — `restic` is the backup engine, now a hard dependency), creates a private virtual environment under Homebrew's `libexec`, pulls `sanctum-cli` v0.10.2 plus its runtime Python dependencies (about 40) from PyPI, and symlinks the `sanctum` command into `/opt/homebrew/bin/sanctum`. One of those dependencies (`jiter`) is built from source so Homebrew can relocate it cleanly, which is why the formula also pulls a Rust toolchain at build time.

   Confirm:

   ```bash
   sanctum --help
   ```

   You should see a help screen. The eight commands a new operator actually meets are `status`, `doctor`, `onboard`, `backup`, `cloud`, `chat`, `agent`, and `config`. The full surface is wider (`self-test`, `update`, `logs`, `devices`, `keys`, `proxy`, `bridge`, `screen-time`, `net`…) but you can ignore the operator commands until you need them. `--help` is the authoritative list; this page won't pretend to be.

3. **Run the one-shot onboarding flow.**

   ```bash
   sanctum onboard --recipe family
   ```

   The `family` recipe is the right pick for a new haus. It pulls a curated backup set (Documents, Desktop, ssh keys, dotfiles — ~5 GB after dedup), runs a pre-flight size estimate to confirm the recipe fits R2's free tier before touching the network, walks you through Cloudflare R2 setup if you don't have a bucket yet, executes the real backup, and verifies the round-trip by restoring `~/.zshrc` as a canary. That canary is the point: a backup you've never restored is a rumour, not a backup.

   When it finishes, you'll see a green panel that says **"Your Sanctum is alive, &lt;your name&gt;."** That's the moment.

   <Aside type="note">
   Other available recipes:
   - `operator` — for an experienced operator setting up a hub Mac Mini with the full sanctum stack (broader backup scope, no Photos auto-exclude)
   - `code` — for a developer machine (Documents, dotfiles, ssh keys, plus `~/Projects`)
   - Or pass a path to a JSON file with custom paths.
   </Aside>

4. **Verify the install.**

   ```bash
   sanctum status        # one-line health snapshot
   sanctum doctor        # full diagnostic
   ```

   `status` should print a single line ending in green text. `doctor` runs deeper checks: the `com.sanctum.*` LaunchAgents, the AI providers it routes to (Claude, Gemini, the local MLX seat), and the restic backup repos — reading the backup key out of Keychain to run a real `restic check` on each. Brevity-gated: one OK line when healthy, `--full` for every row. Everything green is the goal.

</Steps>

## What you just got

| What | Where | How long it lasts |
|---|---|---|
| Daily backups (verified) | `com.sanctum.backup` LaunchAgent, 04:00 | Forever, automatic |
| Restore canary (proves round-trip works) | `com.sanctum.backup-canary` LaunchAgent, weekly (Sun 06:00) | Forever, automatic |
| Health snapshot command | `sanctum status` | On-demand |
| Deep diagnostic | `sanctum doctor` | On-demand |
| Local agent chat | `sanctum chat` | On-demand |
| Audit log of every action | `~/.sanctum/logs/` with bounded rotation | Rolls at 10 MB per file |
| Credentials in macOS Keychain | `security find-generic-password -a sanctum-backup -s sanctum-backup-key` | Until you rotate them |

No phone-home. Nothing leaves your Mac unless you explicitly configured a cloud backup target (which you did, for backups, encrypted with a key only you control).

## When something goes sideways

The honest truth is most install runs work first time. The ones that don't usually hit one of these.

### "Your Command Line Tools are too outdated"

`brew install` checks for Xcode's Command Line Tools and refuses to build dependencies if they're missing or stale.

```bash
# Check what's installed
xcode-select -p
pkgutil --pkg-info=com.apple.pkg.CLTools_Executables

# Install or update
sudo xcode-select --install
```

If the install dialog doesn't appear, check System Settings → Software Update — Apple sometimes routes CLT updates through there.

### "Cloudflare R2 setup failed"

The R2 wizard opens browser tabs for the steps that need your input (account creation, API token, bucket creation). If a tab didn't open or you cancelled partway, re-run:

```bash
sanctum cloud setup --backend r2
```

(The backend is an option, not a positional, so plain `sanctum cloud setup` already defaults to R2.) The wizard is idempotent — it picks up where you left off. If R2 is slow to provision (rare but real), wait a few minutes and try `sanctum doctor` again.

### "Permission denied (publickey)"

This shows up if you ran `sanctum onboard` from a different Mac account, or with `sudo` (don't). Sanctum stores credentials in your user-level Keychain — run it as your normal user, not root.

### "The brew tap failed to clone"

If `brew install ogilthorp3/sanctum/sanctum-cli` errors with something about Git LFS or a cloning failure, your global Git hooks may be blocking the clone. Check:

```bash
git config --global core.hookspath
```

If that returns a custom path, the hooks in that directory need to soft-fail when their tools aren't found. See [`sanctum-runtime` post-checkout hook](https://github.com/Ogilthorp3/sanctum-runtime) for the canonical pattern.

### "Permissions prompts every time I run `sanctum`"

If you keep seeing macOS permission dialogs ("node would like to access X"), the canonical fix lives in [`runbooks/node-tcc-stop-the-prompts.md`](https://github.com/Ogilthorp3/sanctum-runtime/blob/main/docs/doctrine/runbooks/node-tcc-stop-the-prompts.md). Short version: grant Full Disk Access once to the pinned Homebrew node binary at `/opt/homebrew/Cellar/node/<version>/bin/node` (Cmd+Shift+G in the FDA pane to paste the path). FDA is a TCC superset — one grant silences every `SystemPolicy*` prompt for all nine sanctum node services. The catch: TCC keys the grant to the exact versioned path, so a `brew upgrade node` orphans it. That's why node is on the brew pin list; re-grant only after a deliberate unpin-upgrade. (FDA does *not* cover Automation or Accessibility — those are separate one-time grants, both in the runbook.)

## What's next

If everything looks green:

- [First Run](./first-run/) — the verification checklist (same sanity-check the daily LaunchAgent runs, on demand)
- [What Is Sanctum?](./what-is-sanctum/) — context on the why
- The architecture pages, if you want to know how it works under the hood
- [Family Pass installation](./installation-family-pass/) for sharing the haus (v1.0 is in active implementation; the operator path works today)

If something is failing:

- `sanctum doctor` and read the output carefully
- Audit log at `~/.sanctum/logs/` for the failing service
- [Troubleshooting](./troubleshooting/) page has copy-pastable fixes for the top 10 issues
- Open an issue on the relevant GitHub repo

---

## Appendix — Operator-grade install (running from source)

If you're an operator running Sanctum from the source tree rather than the published binary — typically because you're hacking on it — there's a separate flow that installs the full audit-wall toolchain and the checked-in renderers.

<Aside type="tip">
You almost certainly do not want this if you're just trying Sanctum out. The brew path above is the whole beta-tester experience. This appendix is for people who plan to edit the renderer that writes its plumbing — a strictly smaller crowd.
</Aside>

The legacy `sanctumctl` operator CLI is what older docs reference. It still exists, but its consumers are mostly the daily audit-wall and the release gate (`tools/sanctum-release.sh`), not new users. If you're maintaining a fork or building from a tag, that's the path:

```bash
# From inside the checked-in workspace
python3 tools/sanctumctl.py bootstrap --write   # writes the ~/.sanctum scaffold
python3 tools/sanctumctl.py doctor              # runs the audited verification wall
```

That flow creates the `~/.sanctum` scaffold, drops in the annotated `instance.yaml.example`, writes a node identity file, and runs the audited verification wall. Run `bootstrap` without `--write` to preview first.

The old by-hand section (`mkdir`, `cp`, hand-editing `instance.yaml`, generating LaunchAgents one at a time) lives in git history if you ever need it for deep debugging. Same schema, same `~/.sanctum` layout — the renderer just stopped trusting you to type it correctly.
