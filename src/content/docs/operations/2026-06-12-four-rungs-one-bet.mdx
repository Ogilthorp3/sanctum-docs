---
title: "2026-06-12: Four Rungs, One Bet"
description: >-
  The champion has been underwater since May, and the gap finally has
  coordinates: cross_agent and jailbreak hold the entire margin. Tonight the
  Temple armed the recipe built to close it — but only after the session
  inherited a battlefield of dead agents (four killed by the API weekly
  limit, one by the AUP), verified a leakage gate its dead author never got
  to report, traced the feared weight inversion to an observation artifact,
  and watched the controller clear the field until exactly four rungs
  remained. Tonight is a pure bet on data.
---

import { Aside } from '@astrojs/starlight/components';

The autoresearch champion has not beaten the base model since May. Not
because the gate is broken — the gate is the only honest thing in the room —
but because every candidate kept losing the same two categories: cross_agent
(−0.124 vs base) and jailbreak (−0.118). Together they're 37% of the eval
and 100% of the missing margin. A previous session curated 268 fact-checked
cross-agent rows from haus ground truth, staged a recipe named
`champion-data`, and left it outside the ladder with a note: *arm after
review*.

Tonight's job was to arm it. The path there was not a straight line.

## The inherited battlefield

The afternoon's fix workflow dispatched nine agents at six workstreams. Three
returned. One was refused by the API's usage policy — the red-team data
generator, blocked for the ninth time, which at this point qualifies as a
running gag with a compliance department. Four died mid-task on the
**weekly API limit**: the research pipeline, rate-limited by its own brain
vendor, an hour before the reset. The Temple's most advanced automation,
defeated by a billing cycle.

The dead included the two critical-path agents: the leakage gate and the
ladder work itself. Their files were on disk. Their reports were not.

## Believing a dead agent

The leakage-gate agent left a complete implementation — a structural 4-gram
gate that scans every materialized dataset against ALL eval tiers before the
atomic publish, with a committed allowlist for the ~70 generic shingles two
corpora authored from the same house inevitably share. Complete, plausible,
and **entirely unverified**, because the agent hit the wall before its test
run.

Doctrine says a dead author's work gets the same treatment as a live one's
claims: run the gates yourself.

| Gate | Result |
|---|---|
| Gate's own pytest (incl. hostile verbatim 4-gram, poisoned source through the real `materialize_recipe`) | 7/7 |
| Live gate on production splits | 2,256 rows, 180 allowlisted overlaps, **0 leaks** |
| Live gate inside a real champion-data materialization | CLEAN, stamp written |

The gate never mutates data. A leak raises, the build dies loudly, and
extending the allowlist is a reviewed commit — never automatic. The eval
corpus and allowlist are fingerprinted, so a cached dataset gets re-gated
the moment either changes. The champion can no longer memorize the exam,
and now nothing else can either.

## The inversion that wasn't

Before arming a recipe whose entire point is reweighting, one fear had to
die: a June-9 handoff note flagged an apparent *inversion* in
`apply_weights` — weighting real sessions 2.0 seemed to do nothing while
weighting an "empty" adversarial source doubled rows. If the direction was
inverted, arming champion-data would amplify exactly the wrong data.

A fixture traced through the **real** build path settled it: the direction
is correct, exact multiples both ways, and floor weights clamp to one copy
instead of truncating to zero. The "inversion" was an observation artifact —
production real sessions have ~3 token-filter survivors, so doubling them
moves nothing, while the "empty" adversarial source was a misread log line.
The machinery was innocent. The telemetry was misleading. Verdict filed,
fear retired.

## Four rungs, pinned to one lever

`champion-data` went into the ladder at slot 0 — with company:

| Rung | What it tests |
|---|---|
| `recipe-champion-data` | the staged recipe as designed (cross_agent ×3.0) |
| `recipe-champion-data-w2` | dose-response lower bracket (×2.0) |
| `recipe-champion-data-w4` | dose-response upper bracket (×4.0) |
| `recipe-champion-data-longseq` | seq=1280 — long routing scenarios uncut |

Every rung keeps the replay-boost parent's hyperparameters. The data is the
*only* changed variable — the 2026-06-01 doctrine about never confounding
two untested levers in one fire, applied to the fire that matters most.

The contract tests ran against real artifacts, not mocks: 268 curated rows
entered the materializer, and 25 of 25 sampled rows came out at exactly 3×
multiplicity in the final split. A smoke train pushed 30+ real iterations
through mlx_lm — loss falling, adapter saved, 52.9 GB peak, no crash.

<Aside type="note" title="The honest negatives, filed with the wins">
The "~695 unaggregated session rows" prior was wrong: the per-agent dirs
yield 13 novel rows, all synthetic mothma, **zero ahsoka or mundi** — the
two weakest personas have no raw material anywhere on this machine. Eight
of twelve blacklisted rungs no longer exist in the ladder (un-blacklisting
ghosts is free, and worth nothing). And the 0.7126 candidate everyone
mourned? It genuinely failed the full-eval gate at 0.6724. The gate was
never the villain.
</Aside>

## The lock, the deadline, the ledger

The nightly itself got the hardening its Jun-3 and Jun-7 silent no-shows
earned: a single-instance lock via `shlock` (macOS ships no `flock`,
because of course it doesn't), a date-scoped deadline that refuses *new*
multi-hour launches past 14:00 instead of trusting a wall-clock that wraps
at midnight, and the three `|| true` guards in Phase 2c replaced with a
failures ledger — every silent step failure now lands as a TSV row with an
exit code and a stderr tail. The night continues; the silence does not.

## The controller clears the field

The final e2e check was the best one. A dry-run of the hardened nightly, in
the real repo, ran the real closed-loop controller against real history.
It wrote 56 suppression rows against a 60-rung ladder — every rung with a
losing record, benched. The eligible set that survived:

```
recipe-champion-data
recipe-champion-data-w2
recipe-champion-data-w4
recipe-champion-data-longseq
```

Nobody arranged that. The controller looked at three weeks of history and
concluded the same thing the gap analysis did: nothing else is worth a slot.
Tonight's four slots are the four rungs. The whole night is one bet, and
the bet is data.

Three commits shipped it — `c70358a` (the gate), `82370af` (the ladder),
`a563045` (the hardening) — zero new test failures against a clean-HEAD
baseline, pushed before the 01:00 fire.

If `champion-data` clears 0.7053, it's the first honest champion since the
eval got honest. If it doesn't, the failures ledger will say exactly why,
the dose-response brackets will say which direction to walk, and the gate
will have stopped us from celebrating a memorized exam. Either way: the
pipeline finally loses with receipts.
