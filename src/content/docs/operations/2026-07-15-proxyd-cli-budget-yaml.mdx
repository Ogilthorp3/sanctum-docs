---
title: "2026-07-15: Proxyd CLI $50 budget.yaml + green/red banners"
description: Claude CLI shared USD pool, sanctum subs+local only, hot-reload budget.yaml, DEGRADED local context fit. Deployed on manoir.
---

import { Aside } from "@astrojs/starlight/components";

## What landed

Hot-reloaded **`~/.sanctum/sanctum-proxy/budget.yaml`** drives Claude CLI spend ($50/day shared), model pricing, and sanctum seat policy. Route notices are **one green line** in iTerm2; truncated local context is **one red DEGRADED line**.

Full reference: [Proxyd budget.yaml](/architecture/proxyd-budget/).

## Proof

- `cargo test -p sanctum-proxy` — 68 unit tests green (incl. fit_context)
- `test_e2e_budget_route.sh` — health, hot-reload, $50 cap in `/stats`, binary markers
- Live: `cap_usd: 50.0` for agent `claude-code` on manoir `:4040`

## Source

Branch `feat/proxyd-cli-budget-yaml` on sanctum-rs; binary `~/.sanctum/bin/proxyd`.
