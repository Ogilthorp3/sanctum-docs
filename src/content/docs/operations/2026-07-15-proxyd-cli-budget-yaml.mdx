---
title: "2026-07-15: Proxyd CLI budget.yaml + green/red banners"
description: Claude CLI shared USD pool, sanctum subs+local only, hot-reload budget.yaml, DEGRADED local context fit. Deployed on manoir.
---

import { Aside } from "@astrojs/starlight/components";

![A pencil-sketched shared water tank with a marked fill line and several taps drawing from it at once, a card of handwritten rates clipped to its side mid-swap while the taps keep running. A cat sits beside it.](./images/hero-2026-07-15-proxyd-cli-budget-yaml.png)


## What landed

Hot-reloaded **`~/.sanctum/sanctum-proxy/budget.yaml`** drives Claude CLI spend (**$50/day** shared), model pricing, and sanctum seat policy. Route notices are **one green line** in iTerm2; truncated local context is **one red DEGRADED line**. Follow-ups: once-per-prompt banners (PR #29) and free-sub-first fallbacks. A temporary **$150** raise during Max weekly limit was **reverted to $50**.

Full reference: [Proxyd budget.yaml](/architecture/proxyd-budget/).

## Proof

- `cargo test -p sanctum-proxy` — **68 unit tests** green (incl. fit_context)
- `test_e2e_budget_route.sh` — health, hot-reload, cap in `/stats`, binary markers
- Merged to **sanctum-rs `main`**: PR #28 (budget.yaml), PR #29 (banner once-per-prompt)
- Live manoir `:4040`: agent `claude-code` pool active; binary `~/.sanctum/bin/proxyd`

## Deferred (archived tags, not on main)

- `archive/feat-proxyd-presidio-scrub` — deeper OpenRouter PII scrub (Presidio wiring partially on main; full D1 branch archived)
- `archive/feat-proxy-hardening` — mixed WIP (model_scout cert bypass + unrelated mlx/admit) — **not** merge-ready

## Source

**main** only. Deploy: build `sanctum-proxy` from sanctum-rs, install to `~/.sanctum/bin/proxyd`, `pkill -x proxyd` / launchd kickstart.
